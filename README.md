# wordpress-dynamic-menu-shortcodes
WordPress plugin that automatically creates a shortcode for each registered navigation menu, renders menus in a custom hierarchical HTML structure with submenu depth classes, and adds an admin page listing all available shortcodes for easy use in posts, pages, and templates.
